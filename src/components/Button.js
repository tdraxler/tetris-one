export class Button {
  constructor(x, y, sizeX, sizeY, text) {
    this.x = x;
    this.sizeX = sizeX;
    this.sizeY = sizeY;
    this.y = y;
    this.text = text;
  }
  draw(p) {
    
  }
  checkForMouseOver(p) {

  }
  checkForClick(p) {

  }
}